---js
{
  title: "Agenda",
  pagination: {
    data: "collections.events",
    size: 6,
    reverse: true,
    before: function(paginationData, fullData) {
      return this.filterCollectionLang(paginationData, fullData.lang);
    }
  },
  layout: "layouts/page.njk",
  permalink: "/{{ lang }}/agenda/{% if pagination.pageNumber %}{{ pagePrefix }}{{ pagination.pageNumber + 1 }}/{% endif %}",
  eleventyNavigation: {
    key: "Agenda",
    parent: "Actualités",
    order: 2
  }
}
---
<h2 class="fr-h4">{{ "filter_by_tag" | i18n }}</h2>
<div class="fr-mt-6v fr-mb-6w">
  {% set tags = collections.events | filterCollectionLang | getAllTags %}
  {% set tagsUrl = "/agenda/tags/" %}
  {% include "components/taggroup.njk" %}
</div>
{% set postslist = pagination.items %}
{% include "postslist.njk" %}
{% include "templates/pagination.njk" %}
